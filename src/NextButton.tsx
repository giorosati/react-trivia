export default function NextButtton() {
  return <button className="button">Next</button>;
  /*
  logic
  if at front of list disable prev
  if at end of list disable next
  when select prev or next, reset answerState to 0
  
  */
}
